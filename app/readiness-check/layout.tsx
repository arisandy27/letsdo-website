export const metadata = {
  title: "Readiness Check | Let's Do",
  description:
    "Start with a free readiness check to identify the most practical workflow to digitize first.",
};

export default function ReadinessCheckLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
