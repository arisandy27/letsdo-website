export const metadata = {
  title: "Services | Let's Do",
  description:
    "Practical digitalization services to help companies move from manual workflows to structured digital systems.",
};

export default function ServicesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
